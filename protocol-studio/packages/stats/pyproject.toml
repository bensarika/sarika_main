[project]
name = "ps-stats"
version = "0.1.0"
description = "Protocol Studio Trial Lab: deterministic sample-size, allocation-sensitivity and endpoint-exploration routines."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ps_stats"]
